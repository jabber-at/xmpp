Description: remove deps in rebar.config
 Without this patch, rebar would download and compile the listed deps
 at compile time.
Author: Philipp Huebner <debalance@debian.org>

Index: erlang-p1-xmpp/rebar.config
===================================================================
--- erlang-p1-xmpp.orig/rebar.config
+++ erlang-p1-xmpp/rebar.config
@@ -31,9 +31,6 @@
 	      {"priv/lib/xmpp_uri.so", ["c_src/xmpp_uri.c"]},
 	      {"priv/lib/xmpp_lang.so", ["c_src/xmpp_lang.c"]}]}.
 
-{deps, [{fast_xml, ".*", {git, "https://github.com/processone/fast_xml", {tag, "1.1.32"}}},
-        {stringprep, ".*", {git, "https://github.com/processone/stringprep", {tag, "1.0.12"}}},
-        {p1_utils, ".*", {git, "https://github.com/processone/p1_utils", {tag, "1.0.12"}}}]}.
 
 {clean_files, ["c_src/jid.gcda", "c_src/jid.gcno"]}.
 
